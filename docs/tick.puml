@startuml tick
title 服务器单帧（dt = 1/30s）流程

start
:应用输入(转向目标/冲刺/跳跃);
:所有蛇前进 step = speed*dt\n里程计 odo += step\n轨迹头部插入新点、尾部裁剪;
:按弧长 i*间距 采样出每颗珠的世界坐标;
:道具拾取判定\n头部插入对应颜色珠;
partition 碰撞判定 {
  :① 头对头\n比较双方"正前方程度" dot(dir, 指向对方);
  if (差值 < 平局阈值?) then (是)
    :双方各消一颗头珠;
  else (否)
    :胜者消一颗头珠\n败者死亡重生;
  endif
  :② 头撞他人身体第 k 节\n被撞者保留 0..k-1\n撞击者拼接反转的断尾, 新头 = 对方尾端;
  :③ 头撞自身(index >= 阈值) -> 死亡重生;
}
:三消结算(连锁直到无 >=3 同色);
if (长度 == 0?) then (是)
  :奖杯 +1, 广播, 重生;
endif
:补充道具至配置数量;
:序列化快照 + 事件, 广播全部客户端;
stop
@enduml
